services:
  backend:
    # image: zimengxiong/excalidash-backend:0.6.0-dev
    build:
      context: ./backend
    container_name: excalidash-backend
    environment:
      - DATABASE_PROVIDER=sqlite
      - DATABASE_URL=file:/app/prisma/dev.db
      - PORT=8000
      - NODE_ENV=production
      - AUTH_MODE=${AUTH_MODE:-local}
      # Keep disabled by default; only enable when a trusted proxy sanitizes forwarded headers.
      - TRUST_PROXY=false
      # Optional for single-instance deployments:
      # if unset, backend auto-generates and persists one in the volume.
      # Recommended to set explicitly for portability and multi-instance setups.
      - JWT_SECRET=${JWT_SECRET}
      - CSRF_SECRET=${CSRF_SECRET}
      - FRONTEND_URL=${FRONTEND_URL}
      # Native scheduled SQLite backups: the backend takes a consistent snapshot
      # (SQLite online-backup API) on this cron schedule and prunes by retention.
      # The backup-cron service versions the newest snapshot off-site in git.
      - BACKUP_SCHEDULE=${BACKUP_SCHEDULE:-*/5 * * * *}
      - BACKUP_DIR=/app/backups
      - BACKUP_RETENTION_DAYS=${BACKUP_RETENTION_DAYS:-14}
      # Optional OIDC settings (required for AUTH_MODE=hybrid or oidc_enforced)
      # - OIDC_PROVIDER_NAME=Authentik
      # - OIDC_ISSUER_URL=https://auth.example.com/application/o/excalidash/
      # - OIDC_CLIENT_ID=your-client-id
      # - OIDC_CLIENT_SECRET=your-client-secret
      # - OIDC_REDIRECT_URI=https://excalidash.example.com/api/auth/oidc/callback
    volumes:
      - backend-data:/app/prisma
      - backup-snapshots:/app/backups
    networks:
      - excalidash-network
    restart: unless-stopped
    healthcheck:
      # Use IPv4 loopback explicitly to avoid IPv6 localhost resolution issues.
      test:
        [
          "CMD",
          "node",
          "-e",
          "require('http').get('http://127.0.0.1:8000/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))",
        ]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    image: zimengxiong/excalidash-frontend:0.6.0-dev

    container_name: excalidash-frontend
    ports:
      - "6767:80"
    depends_on:
      - backend
    networks:
      - excalidash-network
    restart: unless-stopped
    healthcheck:
      # Use IPv4 loopback explicitly to avoid IPv6 localhost resolution issues.
      test:
        [
          "CMD",
          "wget",
          "--quiet",
          "--tries=1",
          "--spider",
          "http://127.0.0.1:80",
        ]
      interval: 30s
      timeout: 10s
      retries: 3

  backup-cron:
    build:
      context: ./cron-backup
    container_name: excalidash-backup-cron
    environment:
      - GITHUB_OAUTH_TOKEN=${GITHUB_OAUTH_TOKEN}
      - GITHUB_REPO=${GITHUB_REPO}
      # Read the backend's native snapshots from the shared volume (mounted below).
      - SNAPSHOT_DIR=/snapshots
      - BACKUP_TAG=${BACKUP_TAG:-}
      - CRON_SCHEDULE=${CRON_SCHEDULE:-*/5 * * * *}
    depends_on:
      - backend
    networks:
      - excalidash-network
    volumes:
      - backup-cron-data:/var/log
      # Backend writes SQLite snapshots here; the cron container only reads them.
      - backup-snapshots:/snapshots:ro
    restart: unless-stopped

networks:
  excalidash-network:
    driver: bridge

volumes:
  backend-data:
  backup-cron-data:
  # Shared between backend (writer) and backup-cron (reader) for DB snapshots.
  backup-snapshots:
